# derekthomaswood.com - version 1

What has to be true before the footer's `SITE_VERSION` (`includes/config.php`) goes from 0.9 to 1.0. The bar: the site is A+ as a candidate-review surface. Anything that can wait until after launch lives in `backlog.md`. Topic plans keep their own homes (`notes/journal-plan.md`, `notes/journal-ideas.md`, `notes/timeline-content-plan.md`, `notes/proof-plan.md`).

How the writing work runs: one thing at a time, commit after each. Name the thing, the file, and its goal. Derek types in his editor, spelling gets fixed, punctuation is his.

## Most pressing

- [ ] **Finish the PE code editor case study and link it** (Derek, 2026-09-24). It's a mostly finished, "pretty official" case study on the PE site that needs "some more work before it can be read - but not too much." Until it ships, the card's Read more ends on the PE story as a stand-in (https://perpetual.education/stories/building-the-codestudy-sandbox/). When it ships, that door becomes "Read the case study →". Door status and the second candidate door (the journal entry "Taking our code editor to a second site") are in `notes/proof-plan.md`. Open: which repo the PE site is, and what the "more work" is.
- [ ] **Featured cards - add them as each one is ready** (Derek, 2026-09-24). The top 4-5 things to look at stand out visually inside the timeline, so they stay in the story but are easy to find. A milestone gets `"featured": true` in `content/milestones.json` - it only means "stand out," not "has a case study" (not every featured card will be one; a case-study link is its own key, later). Each character decides how it stands out (emphasis tokens that `milestone.css` reads on featured cards, set per character block). Not built yet: the key, the attribute, and the tokens land with the first featured card.

## Resume page

- [ ] **Finish the resume page** (`/resume`: `templates/pages/resume.php` + `styles/modules/resume-index.css` - deliberately separate from the core resume pages, reads nothing they're built from). It has to tell the story of why there are three options and which one fits which scope.
  - **Why it was dragging the site down (2026-09-24):** no clear design, and some AI filler. The filler subtitle ("Fifteen years of the same career, told three ways.") is deleted.
  - **Derek's sketch (2026-09-24)**, top to bottom: (1) the intro - why the experience stacks up like this, and why that fits different roles depending on company size and scope; (2) the stack - the staircase of expandable layers, oldest at the bottom (Art + technical education) up to "What's next?"; (3) the three lanes, each with "For roles like" and a "Generic cover letter" link; (4) a shared CTA module we can reuse on other pages.
  - **Built 2026-09-24:** the stack (`includes/layered-experience-chart.php`, content in `content/layered-experience.json`, placed by `styles/modules/resume-index.css`, palette every other color), the lanes list (lane names as `strong-voice` headings, roles at body voice, the "Generic cover letter" label), and a two-paragraph placeholder intro written straight in the template - Derek replaces it there.
  - **Still Derek's:** the intro (in `templates/pages/resume.php`), what goes inside each layer (`content/layered-experience.json`), and the CTA module (content first, then we build it).
  - Open: should the bar labels be regular weight and larger, like the sketch? The chart's every-other palette was only checked in Quiet light.

## Copy

- [ ] **Sentence pass, in order:** Now page (`templates/pages/now.php` - is it current? does the last line box the search into education?) -> Contact (`templates/pages/contact.php`, likely a quick OK) -> home intro (`templates/pages/home.php`) -> the "Now interviewing" card -> the 16 weight-1 timeline cards in `content/milestones.json`, a few at a time.
- [ ] **Skills chips + descriptions for the other 20 cards** (Derek, 2026-09-24). The chips (`skills` in `content/milestones.json`, words in `content/skills.json`) only got a quick look on the 16 weight-1 cards. The other 20 (weights 2-6) need their chips gone over and their `description`s revisited.
- [ ] **AI-filler audit - Derek reads through the copy himself.** The 2026-09-15 find: the "three lenses / pick the one that matches your role" paragraph on the opener milestone was AI-inserted, sounded off, and linked to a resume page that wasn't ready. That pattern is likely elsewhere in `content/milestones.json`, journal entries, and other copy. Not a grep-and-scrub task - a read-through-and-catch-the-voice task.
- [ ] **Past-writing shelf** (`templates/pages/journal.php`): titles and links only right now. Derek writes the blurbs in one sitting, or it ships bare - either way, a conscious call.
- [ ] **The home page's `goal` brief line** gets rewritten by Derek when the home page changes (it prints to the browser console).

## Journal

- [ ] **Record the "Officially looking for a new long-term role" video** (Derek). Send the Vimeo id; it embeds in `templates/journal/officially-looking.php` the same way as the other two entries. The entry is title + summary until then.
- [ ] **Read the rest of the journal entries.** The first ~16-20 were checked; the rest probably have some filler. Needs a nice relaxing sit.
- [ ] **Journal skeletons.** Every visible journal post should be at least semi-complete - a good skeleton. Some are just filler right now, which is embarrassing. Derek can make videos, take screenshots, do the writing; it doesn't take that long.

## Small screens / mobile

Most reviewers will look at this on a phone.

- [ ] **Small-screen check pass** - go through the site on a phone, no major annoying problems anywhere.
- [ ] **Bleed decision.** Images bleed on small screens right now, which is cool. The alternative is recreating square (or other-ratio) images for every entry, which means a background color per theme - a whole new concern for every theme. Might be more work than it gives us. Decide: keep the bleed, or commit to the per-ratio-per-theme system.
- [ ] **Menu breakpoints** - some look incorrect (whether the menu is on top or in the gutter). Double-check them.

## Navigation / structure

- [ ] **A visible way home, top-left, on every page.** The menu has no real home button. The idea on the table is the sheriffderek circle (`notes/home-circle-idea.md`); the cheap first step is a favicon - the site has none. The fuller circle behavior is in `backlog.md`.
- [ ] **Layout consistency across page types.** Home is extra-wide (fine). Everything else should share one layout - mostly working, but the menu shouldn't bounce around between page types.
- [ ] **Regular one-off pages** - make sure the small pages don't look bad. Now and Contact are fine.

## Themes

- [ ] **Every theme dialed in to A+** (Derek, 2026-09-24) - every character x mood x flavor x scheme combo, finalized fonts and colors. Not redoing posters. Live working state, per-cell verdicts: `notes/walk-notes.md`.
- [ ] **Settings styles and menus from C+ to A+** (Derek, 2026-09-24) - the settings panel, the band, the pages menu, and every control in them: they work well, they're tight, and the detail is worth looking at. The app UI is itself a portfolio piece. The rules it has to keep are the app-ui contract at the top of the `[data-ui='app']` block in `styles/modules/settings-panel.css`.
- [ ] **Flip the starting mood back to Expressive when the colors land.** New visitors start in Quiet for now (2026-09-18) - `DEFAULT_MOOD` in `includes/config.php`; its comment lists the three places that move together.
- [ ] **Self-host the fonts** - the Fontshare CDN `<link>` is fine for auditioning, not for launch; trim the unused audition families at the same time (CLAUDE.md, Theme system).

## First impression

- [ ] **Page titles that say what the reader needs** (2026-09-24). One title per page, the same line in the tab, og:title and twitter:title - and it must not open with "Derek Wood:", because iMessage drops that prefix (the full finding is beside SITE_META_TITLE in `includes/config.php`). Home, Now, How I work, Journal, Contact, and the lane pages all share the site-wide headline today, which iMessage shows as "Technical Product Designer at Large" - no name. `/resume` is done ("Resume: Derek Wood, Technical Product Designer"). The words are Derek's; `/share-previews` flags every card iMessage will trim.
- [ ] **Decide what the share images are for** (2026-09-24 - a decision before any build). The link preview is often the first thing someone sees, and it was never designed: pages use the site default, journal entries get a generated title card, and Paul's /hello/ note went out with the default. Work out the cases (the site, a journal entry, a case study, a /hello/ note, a `?target=` page), what each preview needs to say and why, then build only what that answer needs. One option: a /hello/ note with a Vimeo id could use the video's own thumbnail.

## Done

- [x] **One list style, the same everywhere** (2026-09-24). Lists get bullets inside the column's left line, one rule in `styles/setup.css`; structural lists opt out with `role="list"` or zeroed padding.
